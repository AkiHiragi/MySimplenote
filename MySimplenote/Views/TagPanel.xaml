<UserControl x:Class="MySimplenote.Views.TagPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    
    <Border Style="{StaticResource SidebarBorderStyle}" Width="250">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <!-- Заголовок тэгов -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="15,15,15,10">
                <TextBlock Text="Тэги" FontSize="16" Style="{StaticResource SectionHeaderTextStyle}"/>
            </StackPanel>
            
            <!-- Создание нового тэга -->
            <Border Grid.Row="1" Margin="15,0,15,15" Style="{StaticResource SearchBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0" Text="{Binding NewTagName, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource TagInputTextBoxStyle}"/>
                    <Button Grid.Column="1" Content="+" Margin="5,0,0,0"
                            Command="{Binding CreateTagCommand}"
                            Style="{StaticResource CreateButtonStyle}" Width="25" Height="25"/>
                </Grid>
            </Border>
            
            <!-- Список всех тегов -->
            <ListBox Grid.Row="2" ItemsSource="{Binding AllTags}" Margin="15,0,15,15"
                     Style="{StaticResource NotesListBoxStyle}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,5" Margin="0,2">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="12" Height="12" Fill="{Binding Color}" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Name}" Margin="8,0,0,0" Style="{StaticResource TagTextStyle}"/>
                                <Button Content="+" Margin="5,0,0,0" Width="20" Height="20"
                                        Command="{Binding DataContext.AddTagToNoteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource CreateButtonStyle}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <!-- Разделитель -->
            <Border Grid.Row="3" Height="1" Background="{StaticResource BorderBrush}" Margin="15,10"/>
            
            <!-- Теги текущей заметки -->
            <StackPanel Grid.Row="4" Margin="15">
                <TextBlock Text="Теги заметки:" Style="{StaticResource SectionHeaderTextStyle}" Margin="0,0,0,10"/>
                <ItemsControl ItemsSource="{Binding SelectedNote.Tags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{Binding Color}" CornerRadius="12" Padding="8,4" Margin="2">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="11"/>
                                    <Button Content="×" Margin="5,0,0,0" Width="16" Height="16"
                                            Command="{Binding DataContext.RemoveTagFromNoteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource DangerButtonStyle}" FontSize="10"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
