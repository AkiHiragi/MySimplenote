<UserControl
    mc:Ignorable="d"
    x:Class="MySimplenote.Views.TagsPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Style="{DynamicResource SidebarBorderStyle}" Width="250">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!--  Заголовок тэгов  -->
            <StackPanel
                Grid.Row="0"
                Margin="15,15,15,10"
                Orientation="Horizontal">
                <TextBlock
                    FontSize="16"
                    Style="{DynamicResource SectionHeaderTextStyle}"
                    Text="Тэги" />
            </StackPanel>

            <!--  Создание нового тэга  -->
            <Border
                Grid.Row="1"
                Margin="15,0,15,15"
                Style="{DynamicResource SearchBorderStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBox
                        Grid.Column="0"
                        Style="{DynamicResource TagInputTextBoxStyle}"
                        Text="{Binding NewTagName, UpdateSourceTrigger=PropertyChanged}" />
                    <Button
                        Command="{Binding CreateTagCommand}"
                        Content="+"
                        Grid.Column="1"
                        Height="25"
                        Margin="5,0,0,0"
                        Style="{DynamicResource CreateButtonStyle}"
                        Width="25" />
                </Grid>
            </Border>

            <!--  Список всех тегов  -->
            <ListBox
                Grid.Row="2"
                ItemsSource="{Binding AllTags}"
                Margin="15,0,15,15"
                Style="{DynamicResource NotesListBoxStyle}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,2" Padding="10,5">
                            <StackPanel Orientation="Horizontal">
                                <Ellipse
                                    Fill="{Binding Color}"
                                    Height="12"
                                    VerticalAlignment="Center"
                                    Width="12" />
                                <TextBlock
                                    Margin="8,0,0,0"
                                    Style="{DynamicResource TagTextStyle}"
                                    Text="{Binding Name}" />
                                <Button
                                    Command="{Binding DataContext.AddTagToNoteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Content="+"
                                    Height="20"
                                    Margin="5,0,0,0"
                                    Style="{DynamicResource CreateButtonStyle}"
                                    Width="20" />
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

            <!--  Разделитель  -->
            <Border
                Background="{DynamicResource BorderBrush}"
                Grid.Row="3"
                Height="1"
                Margin="15,10" />

            <!--  Теги текущей заметки  -->
            <StackPanel Grid.Row="4" Margin="15">
                <TextBlock
                    Margin="0,0,0,10"
                    Style="{DynamicResource SectionHeaderTextStyle}"
                    Text="Теги заметки:" />
                <ItemsControl ItemsSource="{Binding SelectedNote.Tags}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                Background="{Binding Color}"
                                CornerRadius="12"
                                Margin="2"
                                Padding="8,4">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock
                                        FontSize="11"
                                        Foreground="White"
                                        Text="{Binding Name}" />
                                    <Button
                                        Command="{Binding DataContext.RemoveTagFromNoteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Content="×"
                                        FontSize="10"
                                        Height="16"
                                        Margin="5,0,0,0"
                                        Style="{DynamicResource DangerButtonStyle}"
                                        Width="16" />
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>